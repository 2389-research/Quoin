name: Quoin
options:
  bundleIdPrefix: ai.2389
  deploymentTarget:
    macOS: "14.0"

packages:
  QuoinPackages:
    path: ../..
  Sparkle:
    url: https://github.com/sparkle-project/Sparkle
    from: "2.6.0"

schemes:
  Quoin:
    build:
      targets:
        Quoin: all
        QuoinUITests: [test]
    test:
      targets:
        - QuoinUITests

targets:
  QuoinUITests:
    type: bundle.ui-testing
    platform: macOS
    sources:
      - UITests
    dependencies:
      - target: Quoin
    settings:
      base:
        SWIFT_VERSION: "6.0"
        TEST_TARGET_NAME: Quoin

  Quoin:
    type: application
    platform: macOS
    sources:
      - Sources
      - Assets.xcassets
      - path: Resources
        type: folder
        buildPhase: resources
    dependencies:
      - package: QuoinPackages
        product: QuoinCore
      - package: QuoinPackages
        product: QuoinRender
      - package: Sparkle
        product: Sparkle
    entitlements:
      path: Quoin.entitlements
      properties:
        com.apple.security.app-sandbox: true
        com.apple.security.files.user-selected.read-write: true
        com.apple.security.files.bookmarks.app-scope: true
        # Sparkle's update check is the app's only network traffic.
        com.apple.security.network.client: true
    info:
      path: Info.plist
      properties:
        CFBundleName: Quoin
        CFBundleDisplayName: Quoin
        # Version/build come from build settings so release builds can stamp
        # them from the git tag (see .github/workflows/release.yml). Defaults
        # for local/dev builds live in settings.base below.
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        LSMinimumSystemVersion: "14.0"
        NSHumanReadableCopyright: "© 2026 2389 Research · Chicago, Illinois"
        # Sparkle auto-update (see docs/reference/distribution.md).
        #   • SUFeedURL   → appcast.xml on GitHub Releases, regenerated per release.
        #   • SUPublicEDKey → Quoin's EdDSA public key (keychain account "Quoin";
        #     the private half signs the appcast and never enters the repo).
        SUFeedURL: "https://github.com/2389-research/Quoin/releases/latest/download/appcast.xml"
        SUPublicEDKey: "NubwOPB+LzCQeeUvWk5hCiOvZuNmQj1BIcjn7tGi+kY="
        SUEnableAutomaticChecks: true
        SUScheduledCheckInterval: 86400
        # Sandboxed apps must let Sparkle launch its installer via XPC.
        SUEnableInstallerLauncherService: true
        # quoin:// deep links (issue #31). The action is the URL host and the
        # target is the `path` query item, resolved against — and confined to —
        # the sandboxed library root (QuoinURLScheme). Registering the scheme
        # here is what makes LaunchServices route quoin:// URLs to the app via
        # application(_:open:).
        CFBundleURLTypes:
          - CFBundleURLName: ai.2389.Quoin.deeplink
            CFBundleTypeRole: Viewer
            CFBundleURLSchemes:
              - quoin
        # Services provider (#35): makes Quoin appear in the system Services
        # menu and in other apps' contextual "Services" submenu. macOS reads
        # this array to build the menu item and to route the message to
        # NSApp.servicesProvider (set in AppDelegate). NSMessage is the
        # @objc selector name (sans the :userData:error: suffix); NSSendTypes
        # gates the item to appear only when the sender offers text. No
        # NSReturnTypes — the service creates/opens a document, it returns
        # nothing to the sender. The send type is the MODERN UTI
        # `public.utf8-plain-text` — the SAME string the handler reads
        # (`pboard.string(forType: .string)`, whose raw type is
        # public.utf8-plain-text). The legacy `NSStringPboardType` name usually
        # maps through AppKit's equivalence table, but a sender that advertises
        # only the UTI form can fail to match it, so the declared send type and
        # the read type are kept identical.
        NSServices:
          - NSMenuItem:
              default: New Quoin Document with Selection
            NSMessage: newDocumentWithSelection
            NSPortName: Quoin
            NSSendTypes:
              - public.utf8-plain-text
        # Handoff / current-activity (issue #36). The open document is published
        # as an NSUserActivity of this type; its userInfo carries a quoin://
        # deep link (never an absolute path or bookmark), re-resolved through
        # QuoinURLScheme on the receiving side so the sandbox boundary holds.
        # Declaring the type here is what makes the activity eligible for
        # Handoff and application(_:continue:) delivery.
        NSUserActivityTypes:
          - ai.2389.Quoin.editing
        # Document-type stance (#16). Quoin is a real Markdown editor, so it
        # declares CFBundleTypeRole=Editor for net.daringfireball.markdown — but
        # keeps LSHandlerRank=Alternate so it coexists politely with whatever the
        # user has set as their default Markdown app (it never aggressively
        # steals .md). Plain text is Viewer-only, also Alternate: Quoin can open a
        # .txt handed to it via Open With, but it does NOT claim ownership of
        # plain text (no Editor role, no default-app pressure). Coexistence rules
        # are documented in docs/reference/architecture.md.
        CFBundleDocumentTypes:
          - CFBundleTypeName: Markdown Document
            CFBundleTypeRole: Editor
            LSHandlerRank: Alternate
            LSItemContentTypes:
              - net.daringfireball.markdown
          - CFBundleTypeName: Plain Text Document
            CFBundleTypeRole: Viewer
            LSHandlerRank: Alternate
            LSItemContentTypes:
              - public.plain-text
        UTImportedTypeDeclarations:
          - UTTypeIdentifier: net.daringfireball.markdown
            UTTypeDescription: Markdown Document
            UTTypeConformsTo:
              - public.plain-text
            UTTypeTagSpecification:
              public.filename-extension:
                - md
                - markdown
                - mdown
                - mkd
    settings:
      base:
        SWIFT_VERSION: "6.0"
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        CODE_SIGN_STYLE: Automatic
        ENABLE_HARDENED_RUNTIME: true
        GENERATE_INFOPLIST_FILE: false
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        # Dev-build defaults; release builds override via xcodebuild
        # (MARKETING_VERSION from the tag, CURRENT_PROJECT_VERSION from the
        # commit count — Sparkle compares CFBundleVersion, so it must grow).
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
